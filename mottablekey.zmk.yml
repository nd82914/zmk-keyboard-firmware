name: MOTTABLEKEY
description: ZMK firmware configuration for the MOTTABLEKEY keyboard
version: 1.0.0
build:
  type: zmk
  firmware: mottablekey
  keymap: mottablekey.keymap
  overlay: mottablekey.overlay
  config: mottablekey.conf
  kconfig: Kconfig.defconfig
  shield: Kconfig.shield
  dependencies:
    - zmk: ^1.0.0
    - other-dependency: ^1.0.0